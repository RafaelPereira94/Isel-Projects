"use strict";

function Artists(id, image, name, detailUrl, genres) {
    this.id = id
    this.image = image
    this.name = name
    this.detailUrl = detailUrl
    this.genres = genres
}

module.exports = Artists